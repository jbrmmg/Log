logging:
  level:
    ROOT: INFO
    com.jbr.middletier: DEBUG
    org.hibernate.type.descriptor.sql: TRACE

spring:
  liquibase:
    contexts: dbg

  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true

server:
  port: 13001

log:
  service-name: middletier-logdb-dbg
